version: "3.8"

services:

  backend:
    container_name: backend
    build: ./chatapp
    env_file:
      - ./chatapp/.env
    ports:
      - "8082:8082"

  frontend:
    container_name: frontend
    build: ./chatappFrontend
    ports:
      - "80:80"
    depends_on:
      - backend
